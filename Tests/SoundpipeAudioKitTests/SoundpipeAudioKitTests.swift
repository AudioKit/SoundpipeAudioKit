    import XCTest
    @testable import SoundpipeAudioKit

    final class SoundpipeAudioKitTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(SoundpipeAudioKit().text, "Hello, World!")
        }
    }
